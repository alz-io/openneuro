% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids.R
\name{bids_index}
\alias{bids_index}
\title{Parse BIDS entity information from file paths}
\usage{
bids_index(files)
}
\arguments{
\item{files}{A data frame returned by \code{\link[=list_files]{list_files()}}, or a character vector
of file paths/filenames.}
}
\value{
A \link[tibble:tibble-package]{tibble} with one row per file and
columns: \code{filename}, \code{subject}, \code{session}, \code{task}, \code{acquisition}, \code{run},
\code{processing}, \code{space}, \code{recording}, \code{suffix}, \code{extension}.
}
\description{
Given a file listing from \code{\link[=list_files]{list_files()}}, extract BIDS entities (subject,
session, task, acquisition, run, processing, space, suffix, extension)
from each filename.
}
\examples{
\dontrun{
fls <- list_files("ds000001", recursive = TRUE)
fls_idx <- bids_index(fls)
dplyr::filter(fls_idx, suffix == "bold.nii.gz")
}
}
